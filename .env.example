# Google Gemini API Key (required - primary image generator)
# Get yours at: https://aistudio.google.com
GEMINI_KEY=your-gemini-api-key-here

# FAL.ai API Key (optional - backup image generator)
# Get yours at: https://fal.ai
FAL_KEY=your-fal-api-key-here

# Apify API token (required for Phase 0 — competitor swipe intelligence)
# Used by skills/references/scrape-meta-ads.mjs to pull gym ads from the
# Meta Ad Library via an Apify actor. Get yours at: https://console.apify.com/account/integrations
APIFY_TOKEN=your-apify-token-here

# Apify actor ID for the Meta Ad Library scraper (optional override).
# Defaults to a public Facebook Ad Library actor if unset.
APIFY_ADLIB_ACTOR=curious_coder~facebook-ads-library-scraper

# Meta Marketing API (publishing from the panel — the Meta link page says how to set these up).
# One Meta app links to one business portfolio and every client has its own, so the keys are per gym:
# the gym's folder name, upper-cased, hyphens as underscores, as a suffix. Keys without a suffix are
# shared by every gym that has none of its own. A system-user token from that portfolio; the app id
# and secret from the app dashboard. Tokens never go in a gym's profile file; they live here only.
META_ACCESS_TOKEN_SCULPT_SOCIETY=your-meta-system-user-token-here
META_APP_ID_SCULPT_SOCIETY=your-meta-app-id-here
META_APP_SECRET_SCULPT_SOCIETY=your-meta-app-secret-here
